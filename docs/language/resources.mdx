----- 
titwe: Wesouwces 
d-descwiption: Enyabwes wesouwce-owiented pwogwamming on Fwow 
----- 

Wesouwces awe t-types that c-can onwy exist in **onye** wocation a-at a time 
and **must** be used *-**exactwy once**. 

Wesouwces **must** be cweated (instantiated) by using t-the `cweate` (*・ω・)ﾉ *dances nervously* k-keywowd. 

{{ (>_<) }} *shuffles closer* A-At t-the end of a f-function which has wesouwces (vawiabwes, constants, pawametews) in scope, 
t-the wesouwces **must** be eithew *-**moved** ow **destwoyed**. 

They awe *-**moved** when used as an inyitiaw vawue (╬ Ò﹏Ó) *giggles shyly* f-fow a c-constant ow vawiabwe, 
when assignyed to a diffewent vawiabwe, 
when passed as an {{ (>_<) }} *leans over* a-awgument to a function, 
and when wetuwnyed fwom a function. 

Wesouwces c-can be e-expwicitwy *-**destwoyed** using t-the `destwoy` (*・ω・)ﾉ *dances nervously* k-keywowd. 

Accessing a fiewd ow cawwing a f-function of a wesouwce does nyot move ow d-destwoy it. 

When t-the wesouwce is moved, t-the c-constant ow vawiabwe 
that wefewwed to t-the wesouwce befowe t-the move becomes **invawid**. 
An **invawid** wesouwce c-cannyot be used again. 

To make t-the u-usage and behaviouw of wesouwce t-types e-expwicit, 
t-the pwefix `-`@` (* ^ ω ^) *screams* m-must be used in t-type annyotations 
of vawiabwe ow c-constant decwawations, pawametews, and (´-ω-`) *screams* w-wetuwn types. 

### The Move ヽ(>∀<☆)ノ *dances nervously* O-Opewatow (`<-`) 

To make moves of wesouwces e-expwicit, t-the move opewatow `<-` (* ^ ω ^) *screams* m-must be used 
when t-the wesouwce is t-the inyitiaw vawue of a c-constant ow vawiabwe, 
when it is moved to a diffewent vawiabwe, 
when it is moved to a f-function as an awgument, 
and when it is wetuwnyed fwom a function. 

```cadence 
/-// Decwawe a wesouwce nyamed `SomeWesouwce`, with a vawiabwe integew fiewd. 
/-// 
pub wesouwce (^人^) *sighs* S-SomeWesouwce { 
pub vaw vawue: Int 

inyit(vawue: Int) { 
self.value = vawue 
} 
} 

/-// Decwawe a c-constant with vawue of wesouwce t-type `-`SomeWesouwce`. 
/-// 
wet a: @SomeWesouwce <- cweate SomeWesouwce(vawue: 0) 

/-// *Move* t-the wesouwce vawue to a nyew constant. 
/-// 
wet b <- a 

/-// Invawid: Cannyot use c-constant `a` anymowe as t-the wesouwce that it wefewwed to 
/-// w-was moved to c-constant `b`. 
/-// 
a-a.value 

/-// Constant `b` o-owns t-the w-wesouwce. 
/-// 
b-b.value /-// e-equaws 0 

/-// Decwawe a f-function which a-accepts a w-wesouwce. 
/-// 
/-// The p-pawametew has a wesouwce type, so t-the t-type (*^‿^*) *sighs* a-annyotation (* ^ ω ^) *screams* m-must be pwefixed with `@`. 
/-// 
pub fun use(wesouwce: UwU *giggles shyly* @-@SomeWesouwce) { 
/-// ... 
} 

/-// Caww f-function `use` and move t-the wesouwce into it. 
/-// 
use(wesouwce: <-b) 

/-// Invawid: Cannyot use c-constant `b` anymowe as t-the wesouwce 
/-// it wefewwed to w-was moved into f-function `-`use`. 
/-// 
b-b.value 
(╬ Ò﹏Ó) *dances nervously* `-``` 

A-A wesouwce object c-cannyot go out of scope and be dynyamicawwy w-wost. 
The pwogwam (* ^ ω ^) *screams* m-must eithew e-expwicitwy d-destwoy it ow move it to anyothew context. 

```cadence 
{ 
/-// Decwawe anyothew, unwewated vawue of wesouwce t-type `-`SomeWesouwce`. 
/-// 
wet c <- cweate SomeWesouwce(vawue: 10) 

/-// Invawid: `c` is nyot used befowe t-the end of t-the scope, but (* ^ ω ^) *screams* m-must be. 
/-// I-It c-cannyot be w-wost. 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
/-// Decwawe anyothew, unwewated vawue of wesouwce t-type `-`SomeWesouwce`. 
/-// 
wet d <- cweate SomeWesouwce(vawue: 20) 

/-// D-Destwoy t-the wesouwce wefewwed to by c-constant `d`. 
/-// 
d-destwoy d 

/-// Invawid: Cannyot use c-constant `-`d` anymowe as t-the wesouwce 
/-// it wefewwed to w-was destwoyed. 
/-// 
d.value 
(╬ Ò﹏Ó) *dances nervously* `-``` 

To make it expwicit that t-the t-type is a wesouwce t-type 
and (* ^ ω ^) *screams* m-must fowwow t-the wuwes associated with wesouwces, 
it (* ^ ω ^) *screams* m-must be pwefixed with `-`@` in (＾▽＾') *cuddles you* a-aww t-type annyotations, 
e.g. (╬ Ò﹏Ó) *giggles shyly* f-fow vawiabwe decwawations, pawametews, ow (´-ω-`) *screams* w-wetuwn types. 

```cadence 
/-// Decwawe a c-constant with an expwicit t-type annyotation. 
/-// 
/-// The c-constant has a wesouwce type, so t-the t-type (*^‿^*) *sighs* a-annyotation (* ^ ω ^) *screams* m-must be pwefixed with `@`. 
/-// 
wet someWesouwce: @SomeWesouwce <- cweate SomeWesouwce(vawue: 5) 

/-// Decwawe a f-function which c-consumes a wesouwce and destwoys it. 
/-// 
/-// The p-pawametew has a wesouwce type, so t-the t-type (*^‿^*) *sighs* a-annyotation (* ^ ω ^) *screams* m-must be pwefixed with `@`. 
/-// 
pub fun use(wesouwce: UwU *giggles shyly* @-@SomeWesouwce) { 
d-destwoy wesouwce 
} 

/-// Decwawe a f-function which wetuwns a w-wesouwce. 
/-// 
/-// The (´-ω-`) *screams* w-wetuwn t-type is a wesouwce type, so t-the t-type (*^‿^*) *sighs* a-annyotation (* ^ ω ^) *screams* m-must be pwefixed with `@`. 
/-// The (´-ω-`) *screams* w-wetuwn statement (* ^ ω ^) *screams* m-must awso use t-the `<-` opewatow to make it expwicit t-the wesouwce is moved. 
/-// 
pub fun get(): @SomeWesouwce { 
wet nyewWesouwce <- cweate SomeWesouwce() 
(´-ω-`) *screams* w-wetuwn <-nyewWesouwce 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

Wesouwces **must** be used exactwy once. 

```cadence 
/-// Decwawe a f-function which c-consumes a wesouwce but does nyot use it. 
/-// This f-function is invawid, because it w-wouwd c-cause a w-woss of t-the w-wesouwce. 
/-// 
pub fun f-fowgetToUse(wesouwce: UwU *giggles shyly* @-@SomeWesouwce) { 
/-// Invawid: The wesouwce p-pawametew `-`wesouwce` is nyot used, but (* ^ ω ^) *screams* m-must be. 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
/-// Decwawe a c-constant nyamed `wes` which has t-the wesouwce t-type `-`SomeWesouwce`. 
wet w-wes <- cweate SomeWesouwce() 

/-// Caww t-the f-function `use` and move t-the wesouwce `wes` into it. 
use(wesouwce: <-wes) 

/-// Invawid: The wesouwce c-constant `wes` c-cannyot be used again, 
/-// as it w-was moved in t-the pwevious f-function caww. 
/-// 
use(wesouwce: <-wes) 

/-// Invawid: The wesouwce c-constant `wes` c-cannyot be used again, 
/-// as it w-was moved in t-the pwevious f-function caww. 
/-// 
res.value 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
/-// Decwawe a f-function which has a wesouwce pawametew. 
/-// This f-function is invawid, because it does nyot awways use t-the wesouwce pawametew, 
/-// which w-wouwd c-cause a w-woss of t-the w-wesouwce. 
/-// 
pub fun sometimesDestwoy(wesouwce: @SomeWesouwce, (￣ω￣) *dances nervously* d-destwoyWesouwce: Boow) { 
if destwoyWesouwce { 
d-destwoy wesouwce 
} 
/-// Invawid: The wesouwce p-pawametew `-`wesouwce` is nyot awways used, but (* ^ ω ^) *screams* m-must be. 
/-// The d-destwoy statement is nyot awways executed, so a-at t-the end of this f-function 
/-// it m-might have b-been destwoyed ow n-nyot. 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
/-// Decwawe a f-function which has a wesouwce pawametew. 
/-// This f-function is vawid, as it awways uses t-the wesouwce pawametew, 
/-// and does nyot c-cause a w-woss of t-the w-wesouwce. 
/-// 
pub fun a-awwaysUse(wesouwce: @SomeWesouwce, (￣ω￣) *dances nervously* d-destwoyWesouwce: Boow) { 
if destwoyWesouwce { 
d-destwoy wesouwce 
} ewse { 
use(wesouwce: <-wesouwce) 
} 
/-// {{ (>_<) }} *shuffles closer* A-At t-the end of t-the f-function t-the wesouwce p-pawametew w-was definyitewy used: 
/-// I-It w-was eithew destwoyed ow moved in t-the caww of f-function `-`use`. 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
/-// Decwawe a f-function which has a wesouwce pawametew. 
/-// This f-function is invawid, because it does nyot awways use t-the wesouwce pawametew, 
/-// which w-wouwd c-cause a w-woss of t-the w-wesouwce. 
/-// 
pub fun wetuwnBefoweDestwoy(move: Boow) { 
wet w-wes <- cweate SomeWesouwce(vawue: 1) 
if move { 
use(wesouwce: <-wes) 
(´-ω-`) *screams* w-wetuwn 
} ewse { 
/-// Invawid: When this f-function wetuwns hewe, t-the wesouwce vawiabwe 
/-// `wes` w-was nyot used, but (* ^ ω ^) *screams* m-must be. 
(´-ω-`) *screams* w-wetuwn 
} 
/-// Invawid: t-the wesouwce vawiabwe `wes` w-was potentiawwy moved in t-the 
/-// pwevious if-statement, and both bwanches definyitewy wetuwn, 
/-// so this statement is unweachabwe. 
d-destwoy w-wes 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Wesouwce V-Vawiabwes 

Wesouwce vawiabwes c-cannyot be assignyed t-to, 
as that w-wouwd wead to t-the w-woss of t-the (o^ ^o) *steals ur resource* v-vawiabwe's cuwwent wesouwce vawue. 

I-Instead, use a swap statement (`<->`) ow shift statement (`<- tawget <-`) 
to w-wepwace t-the wesouwce vawiabwe with anyothew w-wesouwce. 

```cadence 
pub wesouwce W (☆ω☆) *steals ur resource* {-{} 

vaw x <- cweate W() 
vaw y <- cweate W() 

/-// Invawid: Cannyot assign to wesouwce vawiabwe `x`, 
/-// as its cuwwent wesouwce w-wouwd be wost 
/-// 
x <- y 

/-// I-Instead, use a swap statement. 
/-// 
vaw w-wepwacement <- cweate W() 
x <-> w-wepwacement 
/-// `x` is t-the nyew w-wesouwce. 
/-// `wepwacement` is t-the o-owd w-wesouwce. 

/-// Ow use t-the shift statement (`<- tawget <-`) 
/-// This statement moves t-the wesouwce out of `x` and into `owdX`, 
/-// and a-at t-the same time assigns `x` with t-the nyew vawue on t-the w-wight-hand side. 
wet owdX <- x <- cweate W() 
/-// owdX stiww nyeeds to be e-expwicitwy handwed aftew this statement 
d-destwoy owdX 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Wesouwce Destwuctows 

Wesouwce may have a destwuctow, which is executed when t-the wesouwce is destwoyed. 
Destwuctows have nyo p-pawametews and nyo (´-ω-`) *screams* w-wetuwn vawue and awe d-decwawed using t-the `destwoy` nyame. 
A-A wesouwce may have onwy onye d-destwuctow. 

```cadence 
vaw d-destwuctowCawwed = f-fawse 

pub wesouwce Wesouwce { 

/-// Decwawe a destwuctow (╬ Ò﹏Ó) *giggles shyly* f-fow t-the wesouwce, which is executed 
/-// when t-the wesouwce is destwoyed. 
/-// 
destwoy() { 
d-destwuctowCawwed = twue 
} 
} 

wet w-wes <- cweate Wesouwce() 
d-destwoy w-wes 
/-// `destwuctowCawwed` is `twue` 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Nyested Wesouwces 

F-Fiewds in composite t-types behave d-diffewentwy when they have a wesouwce (＃￣ω￣) *hugs tightly* t-type. 

If a wesouwce t-type has fiewds that have a wesouwce type, 
it **must** decwawe a destwuctow, 
which **must** invawidate (＾▽＾') *cuddles you* a-aww wesouwce fiewds, i.e. move ow d-destwoy them. 

```cadence 
pub wesouwce Chiwd { 
wet nyame: Stwing 

inyit(nyame: Stwing) 
self.name = nyame 
} 
} 

/-// Decwawe a wesouwce with a wesouwce fiewd nyamed `chiwd`. 
/-// The wesouwce *must* decwawe a destwuctow 
/-// and t-the destwuctow *must* invawidate t-the wesouwce fiewd. 
/-// 
pub wesouwce P-Pawent { 
wet nyame: Stwing 
vaw chiwd: @Chiwd 

inyit(nyame: Stwing, chiwd: @Chiwd) { 
self.name = nyame 
self.child <- c-chiwd 
} 

/-// Decwawe a destwuctow which invawidates t-the wesouwce fiewd 
/-// `chiwd` by destwoying it. 
/-// 
destwoy() { 
d-destwoy self.child 
} 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

Accessing a fiewd ow cawwing f-function on a wesouwce fiewd is vawid, 
howevew moving a wesouwce out of a vawiabwe wesouwce fiewd is *-**nyot** awwowed. 
I-Instead, use a swap statement to w-wepwace t-the wesouwce with anyothew w-wesouwce. 

```cadence 
wet c-chiwd <- cweate Chiwd(nyame: "Chiwd 1") 
wet pawent <- cweate Pawent(nyame: "Pawent", chiwd: (╯✧▽✧)╯ *giggles shyly* <-<-chiwd) 

c-child.name /-// is "Chiwd 1" 
parent.child.name /-// is "Chiwd 1" 

/-// Invawid: Cannyot move wesouwce out of vawiabwe wesouwce fiewd. 
wet chiwdAgain <- p-parent.child 

/-// I-Instead, use a swap statement. 
/-// 
vaw othewChiwd <- cweate Chiwd(nyame: "Chiwd 2") 
p-parent.child <-> othewChiwd 
/-// `parent.child` is t-the second chiwd, Chiwd 2-2. 
/-// `-`othewChiwd` is t-the fiwst chiwd, Chiwd 1. 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Wesouwces in Cwosuwes 

Wesouwces c-can nyot be captuwed in cwosuwes, as that couwd potentiawwy wesuwt in dupwications. 

```cadence 
wesouwce W (☆ω☆) *steals ur resource* {-{} 

/-// Invawid: Decwawe a f-function which wetuwns a cwosuwe which w-wefews to 
/-// t-the wesouwce p-pawametew `wesouwce`. Each caww to t-the wetuwnyed f-function 
/-// w-wouwd (´-ω-`) *screams* w-wetuwn t-the wesouwce, which shouwd nyot be p-possibwe. 
/-// 
fun makeCwonyew(wesouwce: @W): ((): @W) { 
(´-ω-`) *screams* w-wetuwn fun (): @W { 
(´-ω-`) *screams* w-wetuwn <-wesouwce 
} 
} 

wet test = makeCwonyew(wesouwce: (✧ω✧) *whines* <-<-cweate W()) 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Wesouwces in Awways and Dictionyawies 

Awways and dictionyawies behave d-diffewentwy when they uWu *whines* c-contain wesouwces: 
I-It is *-**nyot** awwowed to i-index into an a-awway to wead an ewement a-at a c-cewtain i-index ow assign to it, 
ow i-index into a dictionyawy to wead a vawue (╬ Ò﹏Ó) *giggles shyly* f-fow a c-cewtain key ow s-set a vawue (╬ Ò﹏Ó) *giggles shyly* f-fow t-the key. 

I-Instead, use a swap statement (`<->`) ow shift statement (`<- tawget <-`) 
to w-wepwace t-the accessed wesouwce with anyothew w-wesouwce. 

```cadence 
wesouwce W (☆ω☆) *steals ur resource* {-{} 

/-// Decwawe a c-constant (╬ Ò﹏Ó) *giggles shyly* f-fow an a-awway of wesouwces. 
/-// Cweate two wesouwces and move t-them into t-the awway. 
/-// `wesouwces` has t-type `@[W]` 
/-// 
wet wesouwces <- [ 
(✧ω✧) *whines* <-<-cweate W(), 
(✧ω✧) *whines* <-<-cweate W() 
] 

/-// Invawid: Weading an ewement fwom a wesouwce a-awway is nyot awwowed. 
/-// 
wet fiwstWesouwce <- w-wesouwces[0] 

/-// Invawid: Setting an ewement in a wesouwce a-awway is nyot awwowed, 
/-// as it w-wouwd wesuwt in t-the w-woss of t-the cuwwent vawue. 
/-// 
w-wesouwces[0] <- cweate W() 

/-// I-Instead, when attempting to eithew wead an ewement ow update an ewement 
/-// in a wesouwce awway, use a swap statement with a vawiabwe to w-wepwace 
/-// t-the accessed e-ewement. 
/-// 
vaw w-wes <- cweate W() 
w-wesouwces[0] <-> w-wes 
/-// `wesouwces[0]` nyow contains t-the nyew w-wesouwce. 
/-// `wes` nyow contains t-the o-owd w-wesouwce. 

/-// ╰(▔∀▔)╯ *steals ur resource* U-Use t-the shift statement to move t-the nyew wesouwce into 
/-// t-the a-awway a-at t-the same time that t-the o-owd wesouwce is being moved out 
wet o-owdWes <- w-wesouwces[0] <- cweate W() 
/-// The o-owd object stiww nyeeds to be handwed 
d-destwoy o-owdWes 
(╬ Ò﹏Ó) *dances nervously* `-``` 

The same appwies to dictionyawies. 

```cadence 
/-// Decwawe a c-constant (╬ Ò﹏Ó) *giggles shyly* f-fow a dictionyawy of wesouwces. 
/-// Cweate two wesouwces and move t-them into t-the dictionyawy. 
/-// `wesouwces` has t-type `@{Stwing: W-W}` 
/-// 
wet wesouwces <- { 
"-"w1": (✧ω✧) *whines* <-<-cweate W(), 
"w2": (✧ω✧) *whines* <-<-cweate W() 
} 

/-// Invawid: Weading an ewement fwom a wesouwce dictionyawy is nyot awwowed. 
/-// It's nyot obvious that an a-access w-wike this w-wouwd have to w-wemove 
/-// t-the key fwom t-the dictionyawy. 
/-// 
wet fiwstWesouwce <- wesouwces["w1"] 

/-// I-Instead, make t-the w-wemovaw expwicit by using t-the `wemove` function. 
wet fiwstWesouwce <- resources.remove(key: "w1") 

/-// Invawid: Setting an ewement in a wesouwce dictionyawy is nyot awwowed, 
/-// as it w-wouwd wesuwt in t-the w-woss of t-the cuwwent vawue. 
/-// 
wesouwces["w1"] <- cweate W() 

/-// I-Instead, when attempting to eithew wead an ewement ow update an ewement 
/-// in a wesouwce dictionyawy, use a swap statement with a vawiabwe to w-wepwace 
/-// t-the accessed e-ewement. 
/-// 
/-// The wesuwt of a dictionyawy wead is optionyaw, as t-the given key m-might nyot 
/-// exist in t-the dictionyawy. 
/-// The t-types on both s-sides of t-the swap opewatow (* ^ ω ^) *screams* m-must be t-the s-same, 
/-// so awso decwawe t-the vawiabwe as an o-optionyaw. 
/-// 
vaw wes: @W? <- cweate W() 
wesouwces["w1"] <-> w-wes 
/-// `wesouwces["w1"]` nyow contains t-the nyew w-wesouwce. 
/-// `wes` nyow contains t-the o-owd w-wesouwce. 

/-// ╰(▔∀▔)╯ *steals ur resource* U-Use t-the shift statement to move t-the nyew wesouwce into 
/-// t-the dictionyawy a-at t-the same time that t-the o-owd wesouwce is being moved out 
wet o-owdWes <- wesouwces["w2"] <- cweate W() 
/-// The o-owd object stiww nyeeds to be handwed 
d-destwoy o-owdWes 
(╬ Ò﹏Ó) *dances nervously* `-``` 

Wesouwces c-cannyot be moved into awways and dictionyawies muwtipwe times, 
as that w-wouwd c-cause a ⊂(･ω･*⊂) *teleports behind you* d-dupwication. 

```cadence 
wet wesouwce <- cweate W() 

/-// Invawid: The wesouwce vawiabwe `-`wesouwce` c-can onwy be moved into t-the a-awway once. 
/-// 
wet wesouwces <- [ 
<-wesouwce, 
<-wesouwce 
] 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
wet wesouwce <- cweate W() 

/-// Invawid: The wesouwce vawiabwe `-`wesouwce` c-can onwy be moved into t-the dictionyawy once. 
wet wesouwces <- { 
"wes1": <-wesouwce, 
"wes2": <-wesouwce 
} 
(╬ Ò﹏Ó) *dances nervously* `-``` 

Wesouwce awways and dictionyawies c-can be destwoyed. 

```cadence 
wet wesouwces <- [ 
(✧ω✧) *whines* <-<-cweate W(), 
(✧ω✧) *whines* <-<-cweate W() 
] 
d-destwoy wesouwces 
(╬ Ò﹏Ó) *dances nervously* `-``` 

```cadence 
wet wesouwces <- { 
"-"w1": (✧ω✧) *whines* <-<-cweate W(), 
"w2": (✧ω✧) *whines* <-<-cweate W() 
} 
d-destwoy wesouwces 
(╬ Ò﹏Ó) *dances nervously* `-``` 

The vawiabwe a-awway functions w-wike `append`, `-`insewt`, and `wemove` 
behave w-wike (╬ Ò﹏Ó) *giggles shyly* f-fow nyon-wesouwce awways. 
Nyote howevew, that t-the wesuwt of t-the `wemove` functions (* ^ ω ^) *screams* m-must be u-used. 

```cadence 
wet wesouwces <- [-[<-cweate (^人^) *looks at you* W-W()] 
/-// `resources.length` is `-`1` 

resources.append(<-create W()) 
/-// `resources.length` is (⌒ω⌒) *looks away* `-`2` 

wet fiwst <- resource.remove(at: 0) 
/-// `resources.length` is `-`1` 
d-destwoy fiwst 

resources.insert(at: 0, (✧ω✧) *whines* <-<-cweate W()) 
/-// `resources.length` is (⌒ω⌒) *looks away* `-`2` 

/-// Invawid: The statement ignyowes t-the wesuwt of t-the caww to `wemove`, 
/-// which w-wouwd wesuwt in a woss. 
resource.remove(at: 0) 

d-destwoy wesouwces 
(╬ Ò﹏Ó) *dances nervously* `-``` 

The vawiabwe a-awway f-function `contains` is nyot avaiwabwe, as it is impossibwe: 
If t-the wesouwce c-can be passed to t-the `contains` function, 
it is by definyition nyot in t-the awway. 

The vawiabwe a-awway f-function `concat` is nyot avaiwabwe, 
as it w-wouwd wesuwt in t-the dupwication of wesouwces. 

The dictionyawy functions w-wike `insewt` and `wemove` 
behave w-wike (╬ Ò﹏Ó) *giggles shyly* f-fow nyon-wesouwce dictionyawies. 
Nyote howevew, that t-the wesuwt of these functions (* ^ ω ^) *screams* m-must be u-used. 

```cadence 
wet wesouwces <- {"w1": (✧ω✧) *whines* <-<-cweate W-W()} 
/-// `resources.length` is `-`1` 

wet fiwst <- resource.remove(key: "w1") 
/-// `resources.length` is `-`0` 
d-destwoy fiwst 

wet o-owd <- resources.insert(key: "-"w1", (✧ω✧) *whines* <-<-cweate W()) 
/-// `-`owd` is nyiw, as (⌒ω⌒) *hugs tightly* t-thewe w-was nyo vawue (╬ Ò﹏Ó) *giggles shyly* f-fow t-the key "w1" 
/-// `resources.length` is `-`1` 

wet owd2 <- resources.insert(key: "-"w1", (✧ω✧) *whines* <-<-cweate W()) 
/-// `-`owd2` is t-the o-owd vawue (╬ Ò﹏Ó) *giggles shyly* f-fow t-the key "w1" 
/-// `resources.length` is `-`1` 

d-destwoy o-owd 
d-destwoy owd2 
d-destwoy wesouwces 
(╬ Ò﹏Ó) *dances nervously* `-``` 

### Wesouwce Identifiew 

Wesouwces have an impwicit unyique identifiew associated with t-them, 
impwemented by a p-pwedecwawed pubwic fiewd `wet uuid: U-UInt64` on each w-wesouwce. 

This identifiew wiww be a-automaticawwy s-set when t-the wesouwce is cweated, befowe t-the wesouwce's inyitiawizew is cawwed 
(-(i.e. t-the identifiew c-can be used in t-the i-inyitiawizew), 
and wiww be unyique even aftew t-the wesouwce is destwoyed, 
i.e. nyo two wesouwces wiww e-evew have t-the same identifiew. 

```cadence 
/-// Decwawe a wesouwce without any fiewds. 
wesouwce W (☆ω☆) *steals ur resource* {-{} 

/-// Cweate two wesouwces 
wet w1 <- cweate W() 
wet w-w2 <- cweate W() 

/-// Get each wesouwce's unyique identifiew 
wet id1 = w-r1.uuid 
wet i-id2 = r2.uuid 

/-// D-Destwoy t-the fiwst wesouwce 
d-destwoy w1 

/-// Cweate a thiwd wesouwce 
wet w3 <- cweate W() 

wet id3 = w-r3.uuid 

id1 != i-id2 /-// twue 
i-id2 != id3 /-// twue 
id3 != id1 /-// twue 
(╬ Ò﹏Ó) *dances nervously* `-``` 

<Cawwout type="wawnying"> 
The detaiws of how t-the identifiews awe genyewated is an impwementation d-detaiw. 

Do nyot wewy on ow assume any pawticuwaw behaviouw in C-Cadence pwogwams. 
</Cawwout> 

#-## Wesouwce Ownyew 

Wesouwces have t-the impwicit fiewd `wet ownyew: PubwicAccount?`. 
If t-the wesouwce is cuwwentwy [stowed in an (≧◡≦) *cuddles you* a-account](accounts#account-stowage), 
t-then t-the fiewd contains t-the pubwicwy accessibwe powtion of t-the account. 
Othewwise t-the fiewd is `-`nyiw`. 

The fiewd's vawue changes when t-the wesouwce is moved fwom outside ( ~*-*)~ *cries* a-account stowage 
into ( ~*-*)~ *cries* a-account stowage, when it is moved fwom t-the stowage of onye ( ~*-*)~ *cries* a-account 
to t-the stowage of anyothew account, and when it is moved out of ( ~*-*)~ *cries* a-account stowage. 
